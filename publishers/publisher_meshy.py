import requests
from settings import MESHY_API_KEY, OPENAI_API_KEY

def publish_meshy(task):
    print("🎨 Generating Meshy 3D asset...")

    prompt = "Generate a unique 3D asset concept"
    print("📦 Meshy generation simulated — API endpoint ready.")
